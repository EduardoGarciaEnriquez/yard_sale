---
import Layout from "./Layout.astro";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<Layout title={title}>
  <div class="login">
    <div class="form-container">
      <slot />
    </div>
  </div>
</Layout>
